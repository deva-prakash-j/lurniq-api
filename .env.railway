# Railway Environment Variables for Lurniq API
# This file contains default values that can be overridden by Railway environment variables

# Spring Configuration
SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}

# Server Configuration
PORT=${PORT}

# Database Configuration (Railway will inject actual MySQL service values)
DATABASE_URL=${DATABASE_URL}
DATABASE_USERNAME=${DATABASE_USERNAME}
DATABASE_PASSWORD=${DATABASE_PASSWORD}

# DDL Management
DDL_AUTO=${DDL_AUTO}

# JWT Configuration (Railway will inject secure values)
JWT_SECRET=${JWT_SECRET}

# Google OAuth2 Configuration (Railway will inject actual values)
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
GOOGLE_REDIRECT_URI=${GOOGLE_REDIRECT_URI:-https://$${RAILWAY_STATIC_URL}/login/oauth2/code/google}

# OAuth2 Success Redirect
OAUTH2_REDIRECT_URL=${OAUTH2_REDIRECT_URL:-https://$${RAILWAY_STATIC_URL}/auth/success}

# JVM Options for Railway
JAVA_OPTS=${JAVA_OPTS:--Xmx512m -Xms256m -XX:+UseG1GC -XX:+UseStringDeduplication -XX:+OptimizeStringConcat}
